name: Full Deploy (local sim)

on:
  workflow_dispatch:
    inputs:
      env:
        description: 'dev or prod sim'
        required: true
        default: 'dev'

jobs:
  deploy-all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy IaC sim
        run: |
          echo "Would run deploy-iac"
      - name: Deploy orchestrator
        run: |
          echo "Would run deploy-orchestrator"
      - name: Deploy agents
        run: |
          echo "Would run agent deploys"
